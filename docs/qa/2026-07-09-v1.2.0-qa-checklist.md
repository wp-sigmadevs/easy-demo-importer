# v1.2.0 QA + merge checklist (`wxr-chunking`)

- **Branch:** `wxr-chunking` → merges to `master`, tag `v1.2.0`
- **Status when written:** code/build/unit verified only — **no server QA yet**
- **Blocking gate:** every row in §1–§5 passes on a real server before merge

## 0. Environment matrix

Run the core cases (§1) on each; the rest at least once on env A.

| Env | Host | Notes |
|---|---|---|
| A | Local (no proxy) | baseline; fastest to iterate |
| B | Behind **Cloudflare** (orange-cloud) | reproduces the original 524; the reason this branch exists |
| C | Slow shared host / low FPM `request_terminate_timeout` | reproduces 503 |
| D | Large **WooCommerce** demo (HPOS + lookup tables) | worst-case per-item cost |

## 1. Chunked content import (core)

| # | Case | Expect |
|---|------|--------|
| 1.1 | Import large WC demo on env B | Completes; **no 524/503**; determinate progress bar advances |
| 1.2 | Same import, watch Network tab | `sd_edi_import_xml_batch` re-fires repeatedly, each < gateway limit |
| 1.3 | Kill connection mid-batch (throttle/offline ~20s) | Auto-resumes (≤5 attempts, backoff), then continues — no duplicate posts |
| 1.4 | Reload the page mid-import | Manual **Resume** picks up from the last checkpoint |
| 1.5 | Two imports at once (two tabs) | Second gets "Waiting for previous…" mutex retry, not corruption |
| 1.6 | Filter `sd/edi/enable_chunked_import` → false | Falls back to single-shot; small demo still imports |
| 1.7 | Corrupt/missing `content.xml` | Graceful error surfaced, no fatal, mutex released |
| 1.8 | Verify final counts | Posts, terms, product cats/tags/attributes counts all correct (deferred-count recalc) |
| 1.9 | Nav menus | No duplicate/leftover menus after a resumed run |

## 2. Image regeneration phase (`sd_edi_regenerate_images`)

| # | Case | Expect |
|---|------|--------|
| 2.1 | Import with images, default opts | After content, phase runs; thumbnails present for all imported attachments |
| 2.2 | During content import, inspect an attachment | Original present, **no** `-WxH` sub-sizes yet (deferred); base metadata valid (renders full size) |
| 2.3 | After regen | All registered sizes generated; media library thumbnails render |
| 2.4 | Large media library (100s of images) | Regen re-fires in time-boxed slices; determinate progress; no 524 |
| 2.5 | 524 during regen | Auto-resumes (phase is in `AUTO_RESUME_PHASES`); no re-processing errors |
| 2.6 | `skipImageRegeneration` = true | Phase skipped → straight to customizer; originals only, still render full size |
| 2.7 | `excludeImages` = true | No attachments, regen skipped, no dangling featured images |
| 2.8 | Pre-existing site media | **Untouched** — only this run's attachment IDs regenerated |
| 2.9 | Filter `sd/edi/regen_chunk_seconds` low (e.g. 5) | More slices, still completes |

## 3. Bundled-media import

| # | Case | Expect |
|---|------|--------|
| 3.1 | Demo zip **with** `uploads/`, images on | All images installed from disk; **zero** outbound image requests; fast |
| 3.2 | Same demo, source host behind Cloudflare | Still succeeds (no remote fetch happens) |
| 3.3 | Partial bundle (some files missing) | Missing ones fall back to remote fetch (40s cap); rest local |
| 3.4 | No `uploads/` in zip | Behaves exactly as before (remote fetch) — **existing themes unaffected** |
| 3.5 | `excludeImages` on | No media imported, bundled or remote |
| 3.6 | CDN URL host ≠ base_url | Fallback `YYYY/MM` tail-match resolves the bundled file |
| 3.7 | Post content URLs | Rewritten to the new local uploads URLs (url_remap works) |
| 3.8 | Activity log | "Bundled media detected…" + "N media files imported from the demo package." |
| 3.9 | Filter `sd/edi/importer/bundled_media_enabled` → false | Forces remote fetch even when `uploads/` present |

## 4. Activity log + result screen

| # | Case | Expect |
|---|------|--------|
| 4.1 | Import Log admin page (`#/import_log`) | Runs grouped newest-first; per-run status (success/failed) correct |
| 4.2 | Every phase | Start, per-phase, warnings (skipped images), success/failure all logged |
| 4.3 | Result screen (success) | Inline "Import details" + "View full log ↗" render |
| 4.4 | Result screen (failure) | Same, plus actionable error message + hint |
| 4.5 | Old install (no log table) | `maybeInstall()` creates it lazily; no fatal |
| 4.6 | 7-day-old entries | Pruned on next run |

## 5. Regression / cross-cutting

| # | Case | Expect |
|---|------|--------|
| 5.1 | Customizer / widgets / menus / settings phases | All still run (the empty-`nextPhaseMessage` halt bug stays fixed) |
| 5.2 | Demo **without** customizer.dat | Pipeline does **not** halt at blank result screen |
| 5.3 | DB reset before import | Works; multisite domain-echo confirm if applicable |
| 5.4 | PHP 8.4 | No deprecations/fatals during a full import |
| 5.5 | Multiple-zip (per-demo) theme config | Correct demo dir resolved |

## 6. Merge + tag (only after §1–§5 green, on explicit go-ahead)

```
# nothing pushed until you say so
git checkout master
git merge --no-ff wxr-chunking
git tag -a v1.2.0 -m "v1.2.0"
# push branches + tag only on explicit go-ahead
```

- [ ] Plugin header version = `1.2.0` (already bumped on branch)
- [ ] `readme.txt` stable tag + changelog reflect 1.2.0
- [ ] `languages/*.pot` regenerated (build)
- [ ] CI unit suite green on the merge commit
- [ ] Decide alpha (Phase 4) merge **after** v1.2.0 ships
- [ ] Multisite renumber 1.2.0 → 1.3.0 on the `multi-site` branch (separate task)

## 7. Known non-blockers (v1.3 candidates)

- Bundle-all thumbnail mode (ship pre-generated sizes) — deferred, correctness caveats.
- Abandoned `.sd-edi-regen-state-*` / `.sd-edi-import-state-*` dotfiles not garbage-collected (self-overwrite on re-run; tiny).
- `original_image` (`-scaled`) handling in bundled export — see export-tool task.
