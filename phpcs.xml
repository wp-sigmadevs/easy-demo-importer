<?xml version="1.0"?>
<ruleset name="WordPress Coding Standards">
    <description>A custom set of rules to check for WP projects</description>

    <config name="minimum_supported_wp_version" value="5.5" />

    <rule ref="WordPress" />
    <rule ref="WordPress-Docs" />
    <rule ref="WordPress-Extra" />

    <!-- Show progress, show the error codes for each message (source). -->
    <arg value="ps"/>

    <!-- Check up to 8 files simultaneously. -->
    <arg name="parallel" value="8" />

    <!-- Show results with colors -->
    <arg name="colors"/>

    <!-- Whenever possible, cache the scan results and re-use those for unchanged files on the next scan. -->
    <arg name="cache"/>

    <!-- Only scan PHP files. -->
    <arg name="extensions" value="php"/>

    <!-- Add in some extra rules from other standards. -->
    <rule ref="Generic.CodeAnalysis.UnusedFunctionParameter"/>
    <rule ref="Generic.Commenting.Todo"/>

    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="easy-demo-importer"/>
            </property>
        </properties>
    </rule>

    <!-- Allow short array syntax -->
    <rule ref="Generic.Arrays.DisallowShortArraySyntax.Found">
        <severity>0</severity>
    </rule>
    <rule ref="Generic.Arrays.DisallowLongArraySyntax.Found"/>
    <rule ref="Universal">
        <exclude name="Universal"/>
    </rule>

    <!-- Adds exceptions in files and variables naming conventions. -->
    <rule ref="WordPress">
        <exclude name="WordPress.Files.FileName"/>
        <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
        <exclude name="WordPress.NamingConventions.ValidVariableName"/>
        <exclude name="WordPress.NamingConventions.ValidFunctionName"/>
        <exclude name="Generic.Commenting.DocComment.MissingShort"/>
        <exclude name="Squiz.Commenting.FileComment.Missing"/>
        <exclude name="Squiz.Commenting.FileComment.Missing"/>
        <exclude name="Squiz.Commenting.FileComment.MissingPackageTag"/>
    </rule>

    <!-- Allows hook names in dot/slash notation. -->
    <rule ref="WordPress.NamingConventions.ValidHookName">
        <properties>
            <property name="additionalWordDelimiters" value="."/>
            <property name="additionalWordDelimiters" value="/"/>
        </properties>
    </rule>

    <!-- Defining Cyclomatic Complexity for a more modular approach -->
    <rule ref="Generic.Metrics.CyclomaticComplexity">
        <properties>
            <property name="complexity" value="30"/>
            <property name="absoluteComplexity" value="30"/>
        </properties>
    </rule>

    <!-- Defining Nested Level -->
    <rule ref="Generic.Metrics.NestingLevel">
        <properties>
            <property name="absoluteNestingLevel" value="8"/>
        </properties>
    </rule>

    <!-- Exclude the 'empty' index files from some documentation checks -->
    <rule ref="Squiz.Commenting.FileComment">
        <exclude-pattern>*/index\.php</exclude-pattern>
    </rule>
    <rule ref="Squiz.Commenting.InlineComment.NoSpaceBefore">
        <exclude-pattern>*/index\.php</exclude-pattern>
    </rule>

    <!--
        The sample config is a documented template: the commented-out filter calls and
        Elementor/Fluent Forms stanzas are alternatives the integrator uncomments, not
        dead code left behind. Adding full stops would corrupt the lines being copied.
    -->
    <rule ref="Squiz.Commenting.InlineComment.InvalidEndChar">
        <exclude-pattern>/samples/*</exclude-pattern>
    </rule>
    <rule ref="Squiz.PHP.CommentedOutCode">
        <exclude-pattern>/samples/*</exclude-pattern>
    </rule>

    <!-- Exclude WP Core folders and files from being checked. -->
    <exclude-pattern>/docroot/wp-admin/*</exclude-pattern>
    <exclude-pattern>/docroot/wp-includes/*</exclude-pattern>
    <exclude-pattern>/docroot/wp-*.php</exclude-pattern>
    <exclude-pattern>/docroot/index.php</exclude-pattern>
    <exclude-pattern>/docroot/xmlrpc.php</exclude-pattern>
    <exclude-pattern>/docroot/wp-content/plugins/*</exclude-pattern>

    <!--
        lib/wordpress-importer/ is NOT untouched upstream code. Its strings are
        re-domained to easy-demo-importer and it carries first-party fixes,
        including the imported-SVG stored-XSS fix and the attachment-URL backfill
        queries. It was previously excluded wholesale, which left ~3.4k lines of
        security-sensitive WXR parsing with no PHP linting at all - PHPStan's
        paths: is `inc` only, so nothing else covered it.

        The security and database sniffs report zero violations there today, so
        they are switched back on at no cost. Only the style, metrics and docs
        sniffs that the upstream code legitimately trips are excluded, and only
        for that path.
    -->
    <rule ref="WordPress.PHP.NoSilencedErrors.Discouraged">
        <exclude-pattern>/lib/*</exclude-pattern>
    </rule>
    <rule ref="Generic.PHP.DeprecatedFunctions.Deprecated">
        <exclude-pattern>/lib/*</exclude-pattern>
    </rule>
    <rule ref="Squiz.Commenting.FunctionComment.MissingParamTag">
        <exclude-pattern>/lib/*</exclude-pattern>
    </rule>
    <rule ref="Generic.Metrics.CyclomaticComplexity.MaxExceeded">
        <exclude-pattern>/lib/*</exclude-pattern>
    </rule>
    <rule ref="Generic.Metrics.NestingLevel.TooHigh">
        <exclude-pattern>/lib/*</exclude-pattern>
    </rule>
    <rule ref="PSR2.Methods.MethodDeclaration.Underscore">
        <exclude-pattern>/lib/*</exclude-pattern>
    </rule>

    <!-- Directories and third party library exclusions. -->
    <exclude-pattern>/vendor/*</exclude-pattern>
    <exclude-pattern>/node_modules/*</exclude-pattern>
    <exclude-pattern>/tests/*</exclude-pattern>
    <exclude-pattern>/docker/*</exclude-pattern>

    <!-- Exclude minified Javascript files. -->
    <exclude-pattern>*.min.js</exclude-pattern>

    <file>.</file>
</ruleset>
